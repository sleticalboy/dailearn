<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sleticalboy.dailywork">

    <application
        android:name=".MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.WheelRVActivity" />
        <activity android:name=".ui.activity.DecorationActivity" />
        <activity android:name=".ui.activity.PagerActivity" />
        <activity android:name=".ui.activity.ClassifyActivity" />
        <activity android:name=".ui.activity.LiveCameraActivity" />
        <activity android:name=".ui.activity.PathActivity" />
        <activity android:name=".ui.activity.AlarmActivity" />
        <activity android:name=".ui.activity.AlphaActivity" />
        <activity android:name=".ui.activity.HeaderActivity" />
        <activity android:name=".ui.activity.CustomViewActivity" />
        <activity android:name=".ui.activity.ScrollerActivity" />
        <activity android:name=".ui.activity.ImageConvertUI" />
        <activity
            android:name=".ui.activity.AutoSwitchUI"
            android:label="@string/title_activity_auto_switch_view" />
        <!-- 转场动画 -->
        <activity android:name=".ui.activity.TransitionUI" />
        <!-- 设备管理 -->
        <activity android:name=".devices.DeviceAdminUI" />

        <activity android:name=".ui.activity.ServicePractise" />
        <!-- authenticator for account -->
        <activity
            android:name=".accounts.auth.AuthenticatorActivity"
            android:excludeFromRecents="true">
            <!--
                No intent-filter here! This activity is only ever launched by
                someone who explicitly knows the class name
            -->
        </activity>
        <!--activity for debug-->
        <activity android:name=".debug.DebugUI" />

        <provider
            android:label="@string/contacts_provider_label"
            android:authorities="@string/account_authority"
            android:name=".accounts.ContactsProvider" />
        <provider
            android:label="@string/routines_provider_label"
            android:authorities="@string/routine_authority"
            android:name=".accounts.RoutinesProvider" />

        <receiver
            android:name=".devices.DevicesReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/devices_admin" />
        </receiver>
        <receiver android:name=".GlobalReceiver">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <service
            android:name=".accounts.auth.AuthenticationService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
        <service
            android:name=".accounts.sync.ContactsSyncService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/sync_adapter_accounts" />
        </service>
        <service
            android:name=".accounts.sync.RoutineSyncService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/sync_adapter_routines" />
        </service>
        <!-- service practice -->
        <service android:name=".MyService" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- permission for accounts authenticator -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_SYNC_STATS" />
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.READ_SOCIAL_STREAM" />
    <uses-permission android:name="android.permission.WRITE_SOCIAL_STREAM" />

</manifest>