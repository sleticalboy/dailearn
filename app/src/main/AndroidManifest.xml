<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sleticalboy.dailywork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="com.sleticalboy.dailywork.ui.activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.sleticalboy.dailywork.ui.activity.StockActivity" />
        <activity android:name="com.sleticalboy.dailywork.ui.activity.SmsSenderActivity" />
        <activity android:name=".ui.activity.RefreshActivity" />
        <activity android:name=".ui.activity.PullRefreshActivity"/>
        <activity android:name=".ui.activity.WheelRVActivity"/>
        <activity android:name=".ui.activity.DecorationActivity"/>
    </application>

</manifest>